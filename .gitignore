node_modules/
dist/
*.tsbuildinfo
coverage/
.DS_Store

# Agent session state and worktrees. Local-only until now, which held while
# this repository had one contributor and stops holding the moment it has two.
.claude/
*.db
*.db-wal
*.db-shm
.env
.env.local

# The container's config.json names one machine's address, so it belongs to
# whoever runs it rather than to the repository.
contrib/docker/config/
